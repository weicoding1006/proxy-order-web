# 後端 Docker image 名稱（用於 docker-compose）
# 複製此檔為 .env 並填入實際值
BACKEND_IMAGE=your-backend-image:latest

# 注意：不需要設定 VITE_API_BASE_URL
# Dev 環境：前端 /api/* 請求由 Vite dev server proxy 轉發至 http://localhost:5184
# Test/Prod：前端 /api/* 請求由 Nginx 反向代理轉發至後端容器的 port 8080
